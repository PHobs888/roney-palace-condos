## MLS / Bridge API Status

Bridge MLS API is configured and reachable via a PHP proxy.
Current status: Authorization pending / returning 401.

Server token and application agreement are active.
Awaiting final broker-side authorization confirmation.
